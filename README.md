Программой поддерживаются бинарные операторы

* `+` (сложение),
* `-` (вычитание),
* `*` (умножение),
* `/` (деление)

и функции

* `sin`,
* `cos`,
* `tan`,
* `log`.

Стоит отметить, что это список можно очень легко и быстро расширить
(см. файл `functionList.cpp`).
