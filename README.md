Если строка не соответствует паттерну, то программа выводит
одну строку "No Match".

Если строка соответствует паттерну, то программа выводит
три строки:

1. строку "Match!",
1. две строки, показывающие, где "кусочки" паттерна входят в строку.
